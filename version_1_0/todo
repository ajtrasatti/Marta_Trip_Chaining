union find
find i 
	r = repr(i)
	while ( z != i)
		i = r
		r = repr{i}
	return i

union (i,j)
	r = find(j)
	repr(i) = repr

k-d tree
- double every time

upload trips to drop box

Use a KD tree then 
Union Find

Use a KD tree to match locations